name: CI - Hello World

on:
  push:
    branches: [ "main" ]

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "GitHub Actions está funcionando 🚀"
